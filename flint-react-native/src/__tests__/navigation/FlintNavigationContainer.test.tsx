import { FlintNavigationContainer } from "../../navigation/FlintNavigationContainer";

describe("FlintNavigationContainer", () => {
  it("is exported as a function", () => {
    expect(typeof FlintNavigationContainer).toBe("function");
  });
});
